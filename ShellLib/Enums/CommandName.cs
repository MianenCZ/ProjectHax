using System;
using System.Collections.Generic;
using System.Text;

namespace ShellLib.Enums
{
	internal enum CommandName
	{
		Empty,
		alias,
		cat,
		cd,
		clear,
		cp,
		diff,
		echo,
		grep,
		head,
		ls,
		man,
		mkdir,
		mv,
		pwd,
		rm,
		rmdir,
		source,
		tail,
		touch,
		tr,
		tty,
		wc,
	}
}
